它通过 it++ 增加了迭代器it，但表达式的值是it增加前的值。这意味着erase方法接收到的是当前的迭代器值，因此它将删除当前迭代器指向的元素。在调用 erase 之后，it 已经指向下一个元素了，因为 it++ 使得 it           自增。但由于这个自增是“后置自增”，它的效果是在 erase 方法调用之后生效，这保证了我们传递给 erase        的是要删除的当前元素的迭代器。